spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/limberdb
    username: limber
    password: limber1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
